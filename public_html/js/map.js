function mapTile() {
    this.terrain = null; // terrain type (index)
    this.unit = null; // unit reference
    this.movePointsLeft = 0; // move points for currently selected unit
    this.inMoveRange = false; // for the currently selected unit
    this.inAttackRange = false;  // for the currently selected unit
}

